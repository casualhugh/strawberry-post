Strawberry Post private libraries
=================================

`strawberry_core` is the deliberately platform-neutral C++11 library used by
both ESP32 production adapters and native tests. It must remain free of Arduino,
WebServer, Wi-Fi, LittleFS, and hardware headers. The library uses caller-owned
buffers and callback seams rather than dynamic allocation.
